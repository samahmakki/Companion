<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/bck">

    <ImageButton
        android:id="@+id/imageFlash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="109dp"
        android:background="@drawable/circle_btn"
        android:contentDescription="@string/flash_light"
        android:src="@drawable/switch_off" />

    <Button
        android:id="@+id/buttonEnable"
        style="@style/Widget.AppCompat.Button.Borderless"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/imageFlash"
        android:layout_alignEnd="@+id/imageFlash"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="21dp"
        android:layout_marginBottom="13dp"
        android:padding="16dp"
        android:text="@string/switchOnCamera"
        android:textColor="#FFF"
        android:textSize="30sp"
        android:textStyle="bold" />


</RelativeLayout>